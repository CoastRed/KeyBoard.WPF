<Window x:Class="KeyBoardDemo.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:keyboard="clr-namespace:KeyBoard.WPF.Behavior;assembly=KeyBoard.WPF"
        xmlns:attch="clr-namespace:KeyBoard.WPF.Controls.Attach;assembly=KeyBoard.WPF"
        prism:ViewModelLocator.AutoWireViewModel="True" Focusable="True"
        Title="{Binding Title}" Height="500" Width="800" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" Text="默认数字键盘" FontSize="25" VerticalAlignment="Center" Margin="20 0">
            <i:Interaction.Behaviors>
                <keyboard:NumericKeyboardBehavior/>
            </i:Interaction.Behaviors>
        </TextBox>

        <TextBox Grid.Row="1" Text="数字键盘" FontSize="25" VerticalAlignment="Center" Margin="20 0">
            <i:Interaction.Behaviors>
                <keyboard:NumericKeyboardBehavior Background="Blue" />
            </i:Interaction.Behaviors>
        </TextBox>
        <TextBox Grid.Row="2" Text="全键盘" FontSize="25" VerticalAlignment="Center" Margin="20 0">
            <i:Interaction.Behaviors>
                <keyboard:KeyboardBehavior/>
            </i:Interaction.Behaviors>
        </TextBox>
        <PasswordBox Grid.Row="3" FontSize="25" VerticalAlignment="Center" Margin="20 0">
            <i:Interaction.Behaviors>
                <keyboard:KeyboardBehavior />
            </i:Interaction.Behaviors>
        </PasswordBox>
        <Button Grid.Row="4" Content="按钮"  FontSize="25" VerticalAlignment="Center" Margin="20 0">
            <i:Interaction.Behaviors>
                <keyboard:KeyboardBehavior/>
            </i:Interaction.Behaviors>
        </Button>
        <TextBox Grid.Row="5" Text="R数字键盘" FontSize="25" VerticalAlignment="Center" Margin="20 0">
            <i:Interaction.Behaviors>
                <keyboard:RNumericKeyboardBehavior Background="#EEEEEE"/>
            </i:Interaction.Behaviors>
        </TextBox>
        <TextBox Grid.Row="6" Text="R全键盘" FontSize="25" VerticalAlignment="Center" Margin="20 0">
            <i:Interaction.Behaviors>
                <keyboard:RKeyboardBehavior Width="1280" Height="400"/>
            </i:Interaction.Behaviors>
        </TextBox>

        <DockPanel Grid.Row="7" VerticalAlignment="Stretch" LastChildFill="True">

            <Button DockPanel.Dock="Right" Content="按钮" Margin="10" Width="200" FontSize="30" VerticalAlignment="Stretch"/>
            <TextBox Margin="20 5" FontSize="30" VerticalAlignment="Stretch">
                <i:Interaction.Behaviors>
                    <keyboard:RKeyboardBehavior/>
                </i:Interaction.Behaviors>
            </TextBox>

        </DockPanel>
    </Grid>
</Window>
